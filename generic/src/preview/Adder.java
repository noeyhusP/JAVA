package preview;

public class Adder<T> {
    public T val1;
    public T val2;
    // 원래 T의 위치에 데이터의 자료형이 와야하는데
    // 임의로 지정해둔 후 객체를 생성할 시에 타입을 결정해줌
    // 즉,클래스 내부에서 지정하는 것이 아닌 외부에서 사용자에 의해 지정되는 것

    // public T add()
    // {

    // }
}
